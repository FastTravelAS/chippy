# frozen_string_literal: true

RSpec.describe Chippy do
  it "has a version number" do
    expect(Chippy::VERSION).not_to be_nil
  end

  it "does something useful" do
    expect(true).to be(true)
  end
end
